# Install this ONLY when you're ready to switch from local SQLite to a real
# Postgres database (see README.md > "Switching to Postgres").
#
#   pip install -r requirements-postgres.txt
#
psycopg2-binary==2.9.9
